def is_list_empty(value: list) -> bool:
    """check if the provided list is empty

    Args:
        value (list): [description]

    Returns:
        bool: True or False
    """
    return len(value) == 0
